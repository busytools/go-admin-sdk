Syntax

admin.exe -email="test@test.com" -command="create/delete/update/switch"

COMMAND

create:create user associated with given email
delete:delete user associated with given email
update:update user associated with given email
switch:enable/disable user associated with given email

if -command isn't used then it will display user info (if email is valid)